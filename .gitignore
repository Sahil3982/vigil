# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
coverage.out

# Dependency directories (remove if using modules)
vendor/

# Go workspace (if you're not using modules)
bin/
pkg/

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*.swn
*.tmp
*.log

# OS generated files
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.tar.gz
*.zip

# Generated by Go tooling
go.mod.lock
go.sum

# Local config / cache
.cache/
.gocache/

# Your own binaries (e.g., built vigil executable)
vigil
vigil.exe

# Optional: ignore local dev files
*.local
.env
.config

# GitHub Actions cache (optional, but safe to ignore)
.github/workflows/cache/

# If you ever generate docs or man pages
docs/
man/